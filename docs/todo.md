# 할 일

2026-08-04 기준. 화면 사이드 노트에 흩어져 있던 미정 16건과 프로젝트 미확정 사항을
한자리에 모았다. **정렬 기준은 "누가 결정하는가"** — 사용자 결정이 필요한 것을 먼저 두고,
내가 바로 진행할 수 있는 것을 뒤에 뒀다. 막힌 것과 안 막힌 것을 섞어두면 무엇이 지금
가능한지가 안 보인다.

**2026-08-04 사용자 결정으로 미정 16건 중 4건 해소, 12건 남음.** 남은 것 중
사용자 결정에 막힌 항목은 없다.

## 지금 어디까지 왔나

**1단계 스킬은 중단**됐고(근거는 `CLAUDE.md` 0장), 챗봇 화면 기획으로 넘어와 **핵심
경로가 이어졌다.**

```
I1 목록 → (방 열림) → I2 확인 질문 → D1 라운드 → R1 보고서 → I1 완료 표시
```

| 화면 | 채택안 | 상태 |
|---|---|---|
| I1 토론 목록·주제 입력 | 보편 B 대화 목록 | 확정 |
| I2 파랑의 확인 질문 | 보편 A 한 번에 하나 | 확정 |
| D1 라운드 진행 | 실험 B 라이브·끼어들기 | 확정 |
| R1 최종 보고서 | 보편 A 파랑이 순차 발송 | 확정 |
| D2 개입 상세 · H1 기록 · S1 설정 | — | 미착수 |

---

## 1. 사용자 결정 — 2026-08-04 전부 결정됨

### 1-1. 챗봇 영속화 계층 — 확정

**로컬 SQLite · 무기한 보관 · 대화 전문 + 라운드별 상태 스냅샷(살아있는 풀·점수
이력·계보·보류 항목) 둘 다 저장.** 재개는 스냅샷으로, 열람은 전문으로. 서버 저장은
공유 기능이 필요해질 때 재검토.

이 결정으로 묶여 있던 화면 미정 3건이 풀렸다:

- I1 「중단된 토론의 수명」 — **해소.** 무기한 보관이므로 수명 개념 자체가 없다
- I1 「완료된 토론의 자리」 — **막힘 해제.** 자동 삭제로는 못 풀게 됐고, 분리/접기의
  순수 화면 설계 문제가 됐다 (→ 2장)
- R1 「내보내기 형식」 — **Markdown 확정.** 로컬 저장이라 링크 공유는 성립하지 않는다

### 1-2. 「모자 지목」 — 기능 제거로 확정

규칙(파랑 검토 후 반영)과 버튼 기대(즉시 발언)의 충돌을 **칩 제거**로 해소했다.
특정 모자의 의견이 필요하면 자유 발언으로 쓰면 파랑이 라운드 설계에 반영한다.
D1.html에서 칩과 미정 항목을 제거했다 (2026-08-04).

### 1-3. MCP 착수 시점 — 챗봇이 완벽하게 동작한 뒤

**챗봇 → MCP 순서 확정. 스킬은 실패로 종결** (2026-08-04 사용자 결정).
tool 후보 목록은 [tool-ideas.md](tool-ideas.md), 1순위는 채점 기록·집계 tool.
그 핵심인 **"이미 낸 점수의 소급 수정 불가"** 는 코드가 기록을 쥐어야 성립한다.
채점 기록은 챗봇 DB 스키마의 일부이므로, 영속화 스키마 설계 때 tool 계약과 같은
구조를 쓴다.

---

## 2. 결정 없이 진행할 수 있는 것

- **D2 개입 상세** — D1의 개입 수단을 펼친 화면. 1-2 결정(모자 지목 제거)으로
  막힘이 풀렸다
- **H1 토론 기록** — I1 목록과 겹치는 부분이 크다. 별도 화면이 필요한지부터 검토
- **S1 설정** — 모델·검색 사용 여부 등. 비공개 필드(모델명·비용) 노출 금지 규칙에 걸림
- **I1 완료된 토론의 자리** — 분리할지 접을지. 무기한 보관 확정으로 순수 화면 문제가 됨
- **반응형 확인** — 지금까지 375 기준으로만 그렸다. 768 / 1280 을 실제로 띄워 확인.
  특히 D1·R1의 1024 이상 레이아웃은 "단일 컬럼 확대"로 두었고 우측 고정 패널 안이 미결
- **I1 목록 항목 관리** — 삭제·이름 변경 동작이 없다

---

## 3. 화면별 미정 — 12건 남음 (16건 중 4건 해소)

각 화면 사이드 노트의 「미정」을 모은 것. **막힌 이유**가 화면 설계면 내가 풀 수 있고,
도메인·정책이면 결정이 필요하다. 2026-08-04 해소된 4건은 취소선으로 남긴다.

| 화면 | 미정 | 막힌 이유 |
|---|---|---|
| D1 | 발언 속도 (사용자 조절 여부) | 화면 |
| D1 | ~~「모자 지목」의 강제력~~ | **해소 — 기능 제거 (1-2)** |
| D1 | 라이브 중 위로 스크롤했을 때의 복귀 | 화면 |
| D1 | 1024 이상 레이아웃 | 화면 |
| I2 | 질문 수 예고(`1/4`)의 정확성 — 답에 따라 질문이 줄면 숫자가 흔들림 | 화면 |
| I2 | 「고칠 데 있음」의 동작 (부분 수정 / 처음부터) | 화면 |
| I2 | 주제 심사의 자리 — 확인 질문 전이면 문지기, 후면 이미 답 받은 뒤 | 화면 |
| I2 | 답이 전제를 흔들 때의 되돌림 — 규칙엔 있는데 화면이 없음 | 화면 |
| I1 | 완료된 토론의 자리 (분리/접기) | 화면 (1-1로 막힘 해제) |
| I1 | ~~중단된 토론의 수명~~ | **해소 — 무기한 보관 (1-1)** |
| I1 | 삭제·이름 변경 | 화면 |
| I1 | 주제 심사를 I2 어디에 넣을지 | 화면 (I2와 같은 건) |
| R1 | ~~내보내기 형식~~ | **해소 — Markdown (1-1)** |
| R1 | ~~「더 토론하기」의 라운드 번호~~ | **해소 — 기능 제거, 「새 토론」으로 대체** |
| R1 | 풀에 여럿 남은 채 끝난 경우의 우선순위 화면 | 화면 |
| R1 | 보고서 생성 시간 — 6장을 만드는 실제 소요 미측정 | 구현 |

---

## 4. 보류·중단

- **스킬 (1단계) — 실패로 종결** (2026-08-03 중단, 08-04 실패 판정). 파일은
  `.claude/skills/six-hats/` 에 남아 있으나 더 투자하지 않는다. 이유는 `CLAUDE.md`
  0장. 되살리려면 git 이력에 전부 있다.
- **검증 로그 규칙 — 만들었으나 아직 안 씀.** `docs/verification/{날짜}-{주제}.md`
  로 원본을 남기기로 했는데(2026-08-02), 그 뒤 스킬을 중단해 쓸 일이 없었다.
  챗봇이 돌기 시작하면 같은 규칙을 적용한다 — **비교 기준이 없으면 개선을 증명할 수 없다.**
- **옛 화면 산출물** — 커밋 `561ef80` 의 `docs/screens/` 는 모자 탈락제 시절 것이다.
  `wireframe.css` 만 살려 재사용했고 나머지는 참고하지 말 것.
