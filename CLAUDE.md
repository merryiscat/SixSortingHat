# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 0. 현재 상태

- **스킬 (1단계)**: `.claude/skills/six-hats/` — SKILL.md + hats.md + scoring.md
  + report.md. 트리거 "사고 확장".
  실제 주제로 2라운드 돌려 **모자 탈락제의 결함을 확인하고 아이디어 탈락제로 재설계**
  했습니다 (2026-07-28, 근거는 2장 말미).
  **재설계본 검증 완료 (2026-07-29)** — 같은 주제("논문 분석·번역·리뷰 도구")로 전체
  사이클(확인 질문 → 3라운드 → 최종 보고서)을 완주했습니다. 구버전의 결함이던 모드
  붕괴가 재현되지 않았고(축 분담 채점 효과), 진전의 대부분이 병합에서 나왔습니다
  (병합 3건 vs 탈락 1건). 검증에서 발견된 규칙 공백은 2026-07-29 보강 완료 —
  동점 처리 우선순위, 복수 병합 허용 조건, 병합 견제(연속 최하위는 탈락 우선·흡수
  기여 명시·미검증 부분 보류), 하양의 "확인 필요" 검색 해소 의무.
  같은 날 **파랑 지휘형으로 개편** (사용자 결정) — 의무 탈락은 라운드 1의 1회로
  줄이고, 라운드 2부터는 파랑이 확장·추가·제거·검증 등 방식을 자유 설계. 종료는
  파랑 제안 + 사용자 승인(최대 6라운드), 하양은 매 라운드 검색 의무 1회.
  **지휘형 개편본 검증 완료 (2026-07-29, 10개 주제 병렬 실행)** — 10/10 완주(4~5라운드,
  전부 파랑 종료 제안), 검색 총 63회로 매 라운드 의무 준수, 견제 규칙 실발동 확인.
  발견된 공백은 같은 날 보강 완료 — 파랑 대행 채점 기준(담당 모자 관점으로, "(대행)"
  표기), 겸행 라운드 공식 허용, 부분 병합 공식화, R1 중복 씨앗 병합 허용, 검색
  실패/미발견 구분. **관찰 항목으로 남긴 것**: "추가" 라운드가 10회 중 0회(기능 사장
  여부), R2가 거의 항상 검증으로 고정되는 관성, 최종 가중합이 7.65~8.50 좁은 구간에
  몰리는 자기일관성 편향(스킬 형태의 구조적 한계, 챗봇 단계에서 모델 분리로 검증).
  **전체 검토 후 보강 (2026-08-02)** — 검증이 아니라 문서 정합성 점검에서 나온
  수정입니다. (1) 출력 형식의 이모지 제거 — 글로벌 규칙 위반이자 채점표 정렬 파손
  요인. (2) "모드 이탈"의 정의 수정 — "하양이 아이디어를 만들면 이탈"이라는 문구가
  하양의 씨앗 규정과 모순이어서, **무엇을 내느냐가 아니라 어느 모드에서 출발했느냐**로
  기준을 바꿨습니다. (3) 대행 채점을 "라운드 1 한정"에서 **"이번 라운드에 태어난
  씨앗인가"** 기준으로 확장 — "추가" 라운드에서 자기 씨앗을 자기가 채점하는 구멍이
  있었습니다. (4) 「도구 사용」 절 신설 — 사용자 자료 읽기(하양 책임)·계산 실행·난수.
  (5) 라운드 유형 관성 견제 — 풀이 3개 이하면 "추가"를 후보에 필수 포함.
  **이 보강본은 아직 실행 검증을 하지 않았습니다.** 다음 실행이 곧 검증입니다.
- **검증 로그**: 남아 있지 않습니다. 두 차례 검증 결과가 이 문서의 요약 몇 줄로만
  존재하고 원본 산출물(라운드별 점수·계보)이 없어, **규칙을 바꿨을 때 이전과 비교할
  기준이 없습니다.** 다음 검증부터는 `docs/verification/{날짜}-{주제}.md` 로 원본을
  남기십시오. 특히 라운드별 가중합 수치는 자기일관성 편향 재측정의 유일한 근거입니다.
- **스킬 단계 중단 (2026-08-03 사용자 결정)**. 파일은 지우지 않고 남겨두되 **더
  투자하지 않습니다.** 세 번째 검증(탈노션 메모 앱, 3라운드)에서 나온 판정입니다.
  - **내용은 값이 있었습니다** — 라운드 3에서 설계 결정 4건이 나왔고, 그중 하나는
    앞선 두 라운드가 틀린 문제(본문 충돌)에 쓰였다는 자기 반증이었습니다.
  - **그러나 6모자가 그것을 만들었는지는 증명되지 않았습니다.** 통찰은 사용자가
    "얻는 게 없다"고 지적한 뒤 제가 깊이 파기로 전환하면서 나왔습니다. 제가 낸
    생각을 다섯 목소리에 배분한 것과 구별할 방법이 스킬 구조에는 없습니다.
    한 모델이 제안자·심판·심층분석가를 겸하기 때문입니다.
  - **점수는 품질이 아니라 주목도를 측정했습니다.** 그 라운드에 조명받은 아이디어
    하나만 +0.95 올랐습니다. 자기일관성 편향이 예정대로 재현됐습니다.
  - **사용자 개입 없이는 좋은 라운드를 못 열었습니다.** 권한·가독성·깊이 세 가지를
    전부 사용자가 먼저 지적했습니다.
  - 결론: 이 질문은 **모델을 분리해야** 답이 나오므로 스킬 단계에서 규칙을 더
    다듬는 것은 답 없는 질문에 시간을 쓰는 것입니다.
- **화면 기획 (진행 중)**: `docs/screens/` — 규약 `README.md` · 공용 CSS
  `wireframe.css`. 형태는 **메신저**입니다 (사용자 결정) — 6모자가 있는 대화방에
  사용자가 참여자로 끼어듭니다.

  | 화면 | 채택안 | 상태 |
  |---|---|---|
  | I1 토론 목록·주제 입력 | 보편 B 대화 목록 | 확정 |
  | I2 파랑의 확인 질문 | 보편 A 한 번에 하나 | 확정 |
  | D1 라운드 진행 | 실험 B 라이브·끼어들기 | 확정 |
  | R1 최종 보고서 | 보편 A 파랑이 순차 발송 | 확정 |
  | D2 개입 상세 · H1 기록 · S1 설정 | — | 미착수 |

  **핵심 경로(시작 → 토론 → 결과)가 이어졌습니다.** 각 화면 사이드 노트에 미정이
  16건 쌓여 있으며, 그중 둘은 다른 미확정 사항에 걸려 있습니다 — I1의 "중단된
  토론의 수명"과 R1의 "내보내기 형식"은 챗봇 영속화 설계(3-1)를 정해야 풀립니다.

  **화면 전환은 I1에서만 일어납니다.** 목록에서 방에 들어간 뒤로는 I2(확인 질문)와
  D1(라운드)이 같은 방에서 이어집니다 — 메신저이므로 시작과 토론이 한 흐름입니다.
  D1의 진행 방식은 **자동 진행하되 입력창을 건드리면 멈춥니다.**
  **옛 화면(커밋 `561ef80`)을 참고하지 마십시오** — 모자 탈락제 시절 것이라 단위와
  점수 체계가 다릅니다. `wireframe.css` 만 재사용했습니다.
  화면 위계는 **새로 알게 된 것 > 아이디어 변화 > 파랑 정리 > 발언 > 점수(접힘)** 이며,
  이 순서는 스킬 검증에서 사용자가 직접 판정한 것입니다.
- **MCP (2단계)·챗봇 (3단계)**: 미착수. 빌드·테스트·실행 명령은 아직 없습니다 —
  없는 명령을 이 문서에 지어내지 마십시오.

---

## 1. 무엇을 만드는가

에드워드 드 보노의 6색 모자 사고법을 LLM에게 적용해 **사용자의 아이디어를 다각도로 확장**하는
것이 목적입니다. 모자별 역할 정의는 `README.md`가 원천입니다.

같은 사고 엔진을 **세 가지 전달 형태**로 만듭니다.

| 서비스 | 형태 | 소비자 |
|---|---|---|
| **6모자 아이디어 확장 챗봇** | 자체 서비스. 6개 에이전트가 순차 발언하는 대화형 UX | 최종 사용자 |
| **아이디어 확장 MCP** | MCP 서버. 6모자 확장을 도구(tool)로 노출 | 임의의 MCP 클라이언트 (Claude Desktop, Claude Code 등) |
| **아이디어 확장 스킬** | Claude Skill (`SKILL.md` + 참조 파일) | Claude 세션 내부 |

세 서비스는 **동일한 6모자 도메인 규칙(2장)을 공유**합니다. 모자 정의·프롬프트·품질 기준을
세 곳에 복붙하지 말고 하나의 원천에서 파생시키십시오. 형태별로 다른 것은 실행 방식뿐입니다.

형태를 가르는 기준은 **"LLM 호출을 누가 하는가"** 입니다.

- **챗봇** — 오케스트레이션을 자체 코드가 수행. 상태 관리·스트리밍·영속화가 필요합니다.
- **MCP** — 오케스트레이션 주체는 호출한 클라이언트의 LLM. 서버는 도구 계약과 프롬프트 자산을 제공합니다.
- **스킬** — 오케스트레이션 주체는 Claude 자신. 산출물은 실행 코드가 아니라 지시문입니다.

무언가를 어디에 둘지 헷갈릴 때 이 기준으로 판단하십시오.

---

## 2. 6모자 도메인 규칙 (세 서비스 공통)

`README.md`의 모자별 역할 위에 아래 규칙이 붙습니다.

1. **하양/빨강/검정/노랑/초록 5개가 사고를 확장하고, 파랑이 진행을 관리·종합합니다.**
   파랑은 다른 모자와 동급이 아니라 지휘자입니다.
2. **발언 순서는 매 실행마다 달라야 합니다.** 고정 순서는 설계 위반입니다.
3. **결과가 품질 기준에 미달하면 같은 질문을 그대로 재실행하지 말고, 쿼리를 확장해서
   재토론합니다.** 확장 없는 재순환은 같은 답을 반복할 뿐입니다.
4. **품질 판정은 구조화 출력(스키마 강제)으로 받습니다.** LLM 자연어에서 접두사를 잘라내는
   문자열 파싱은 금지입니다.
5. **판정 과정에서 예외가 나면 기본 동작은 "통과"가 아니라 "실패"** 입니다. 예외를 통과로
   처리하면 검증이 없는 것과 같습니다.

### 토론 사이클 — 아이디어 탈락제 + 파랑 지휘형 (2026-07-28 재설계, 07-29 개편)

**탈락하는 것은 모자가 아니라 아이디어입니다. 모자는 여섯 전부 끝까지 남습니다.**

0. 파랑이 주제의 모호한 지점을 **사용자에게 되묻고** 답을 받아 주제를 확정합니다.
   이 관문을 건너뛰면 안 됩니다.
1. **라운드 1 (씨앗)** — 발언 모자 5개가 각자의 모드에서 아이디어를 하나씩 냅니다
   (하양=사실이 가리키는 안, 빨강=사람이 원할 것 같은 안, 검정=가장 안전한 안,
   노랑=이득이 가장 큰 안, 초록=틀을 깨는 안). 파랑은 씨앗을 내지 않습니다.
2. 채점 후 최하위 하나가 탈락하고(**의무 탈락은 이 1회뿐**), **아이디어를 제안자와
   분리**합니다. 이후 어떤 아이디어도 "누구의 것"이 아닙니다.
3. **라운드 2 이후 (파랑 지휘)** — 파랑이 매 라운드의 목적·방식을 직접 설계하고
   이유를 밝힙니다: 확장(깊이 파기)·추가(새 씨앗)·제거(탈락·병합)·검증(공격·사실
   확인) 등. 목적을 겹친 겸행 라운드(예: 확장+제거)도 가능합니다. 신규 투입은
   "추가" 라운드에서만, 탈락·병합은 "제거"가 선언된 라운드에서만 (견제 규칙 적용,
   부분 병합 허용).
4. **채점은 축마다 담당 모자 하나**, 매 라운드 말에 살아있는 전부를 매깁니다 —
   근거 0.25(하양) / 실행가능성 0.20(노랑) / 위험내성 0.20(검정) /
   주제적합성 0.15(파랑) / 독창성 0.10(초록) / 호응 0.10(빨강).
   가중합은 소수점 둘째 자리까지.
5. **하양은 매 라운드 최소 1회 검색**합니다 (없으면 "검색 불필요"와 이유 명시).
   파랑은 다른 모자에게 검색을 지시할 수 있습니다.
6. **종료는 파랑 제안 + 사용자 승인** — 더 확장해도 새 것이 나오지 않는다고 판단할
   때. 안전장치로 최대 6라운드. 풀에 여럿 남은 채 끝나면 보고서에서 파랑이
   우선순위를 매깁니다. **여섯 모자와 파랑이 함께** 최종 보고서를 씁니다.
7. 라운드 종료마다 **사용자에게 확인받고 멈춥니다.** 사용자가 방향을 틀 수 있는
   유일한 지점입니다.

**왜 모자 탈락제를 버렸는가** (2라운드 실측 근거):
공통 채점표를 모자에 적용하니 모자들이 그 척도로 수렴해 서로 닮았습니다 — 감정 모자가
UI 설계 처방을 냈습니다. 6모자의 가치는 관점의 차이인데 그 차이가 지워졌습니다. 또한
여섯 관점을 다 쓰자는 기법에서 관점을 하나씩 죽이는 건 취지와 충돌합니다. 축 정의를
세 번 고쳐도 특정 모자의 편향이 안 잡혔는데, 서로 다른 직무의 역할을 하나의 척도로
줄 세우는 게 원리적으로 불가능하기 때문입니다. 실제 진전은 탈락이 아니라 **병합**에서
나왔습니다.

---

## 3. 확정 사항 (2026-07-28 사용자 결정)

- **개발 순서**: ~~스킬 → MCP → 챗봇~~ → **스킬 중단, 챗봇 화면 기획으로 전환**
  (2026-08-03 사용자 결정). 스킬로 도메인 규칙을 검증한다는 계획이었으나, 한 모델이
  전부를 겸하는 구조에서는 검증 자체가 성립하지 않는다는 것이 세 번째 검증에서
  드러났습니다 (0장 참조). 검증된 자산으로 남은 것은 **도메인 규칙(2장)과 화면
  요구사항**이며, 프롬프트·평가 기준은 재검증 대상입니다.
- **저장소 배치**: 모노레포 — 이 저장소 하나에 세 서비스를 함께 둡니다.
- **언어·오케스트레이션**: Python + LangGraph, 패키지 관리는 uv.
- **LLM 모델**: `gpt-5.4-mini` 기본. (역할별 상향이 필요해지면 파랑만 `gpt-5.4` 검토)
- **토론 사이클**: **아이디어** 탈락제 + 파랑 지휘형 (2장 참조). 모자는 탈락하지
  않습니다. 의무 탈락은 라운드 1의 1회, 이후는 파랑이 라운드를 자유 설계.
  종료 조건 = 파랑 제안 + 사용자 승인 (최대 6라운드). — 2026-07-29 사용자 결정.
- **스킬**: 트리거는 "사고 확장". 위치는 `.claude/skills/six-hats/`. 모자들은 웹 검색
  tool을 사용할 수 있고, 추가 tool은 필요 시 사용자가 제공합니다.

## 3-1. 미확정 사항

아래는 아직 정해지지 않았습니다. **임의로 확정하지 말고 사용자에게 확인하십시오.**

- 챗봇의 영속화 계층 필요 여부
- MCP 단계의 커스텀 tool 설계 — 후보 목록은 [docs/tool-ideas.md](docs/tool-ideas.md)
  (1순위: 채점 기록·집계 tool)

---

## 4. 작업 방식

- 작업을 "완료"로 표시하기 전에 **성공 기준을 실제로 실행해서 검증**하십시오.
- 파일명에 `_v2` 같은 버전 접미사를 붙이지 말고 **같은 파일을 직접 갱신**하십시오.
  이력은 git이 관리합니다.
- **Windows 환경 주의** — 파일을 쓸 때 UTF-8로 저장되는지 확인하십시오
  (`Set-Content`/`Out-File`은 `-Encoding utf8` 명시). 인코딩이 어긋나면 `.gitignore` 같은
  파일을 git이 통째로 무시합니다.
- 작업 후 이 `CLAUDE.md`를 최신 상태로 유지합니다.
